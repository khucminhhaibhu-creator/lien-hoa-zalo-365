const state=JSON.parse(localStorage.getItem('lhz365')||'null')||{groups:[],members:[],messages:[]};const $=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function save(){localStorage.setItem('lhz365',JSON.stringify(state));render()}function demo(){state.groups=[{id:'GMF001',name:'Lớp Tiếng Trung 01',status:'Hoạt động',link:''},{id:'GMF002',name:'Khách hàng Liên Hoa',status:'Hoạt động',link:''}];state.members=[{name:'Nguyễn Văn An',uid:'zalo_10001',phone:'0900000001',group:'Lớp Tiếng Trung 01'},{name:'Trần Thị Bình',uid:'zalo_10002',phone:'0900000002',group:'Khách hàng Liên Hoa'}];state.messages=[{time:'20:15',sender:'Nguyễn Văn An',group:'Lớp Tiếng Trung 01',text:'Cho em hỏi lịch học ngày mai?'},{time:'20:18',sender:'Trần Thị Bình',group:'Khách hàng Liên Hoa',text:'Em muốn đăng ký khóa giao tiếp.'}];save();$('msg').textContent='Đã nạp dữ liệu mẫu.'}function addGroup(){const link=$('groupLink').value.trim(),name=$('groupName').value.trim();if(!/^https?:\/\/(www\.)?zalo\.me\/g\/[A-Za-z0-9_-]+/.test(link)){ $('groupMsg').textContent='❌ Link không đúng. Hãy dán dạng https://zalo.me/g/xxxxxxxx';return}const id=link.split('/g/')[1].split(/[?#/]/)[0];if(state.groups.some(g=>g.id===id)){ $('groupMsg').textContent='⚠️ Nhóm này đã có trong danh sách.';return}state.groups.push({id,name:name||'Nhóm Zalo '+id.slice(0,8),link,status:'Đã thêm — chờ API'});$('groupLink').value='';$('groupName').value='';$('groupMsg').textContent='✅ Đã thêm nhóm vào Liên Hoa Zalo 365.';save()}function render(){ $('ng').textContent=state.groups.length;$('nm').textContent=state.members.length;$('nt').textContent=state.messages.length;$('nl').textContent=state.members.length;$('groupsTable').innerHTML=state.groups.map(g=>`<tr><td>${esc(g.name)}</td><td>${esc(g.id)}</td><td>${g.link?`<a href="${esc(g.link)}" target="_blank" rel="noopener">Mở nhóm</a>`:'—'}</td><td>${state.members.filter(m=>m.group===g.name).length}</td><td>${esc(g.status)}</td></tr>`).join('')||'<tr><td colspan="5">Chưa có dữ liệu</td></tr>';$('membersTable').innerHTML=state.members.map(m=>`<tr><td>${esc(m.name)}</td><td>${esc(m.uid)}</td><td>${esc(m.phone)}</td><td>${esc(m.group)}</td></tr>`).join('')||'<tr><td colspan="4">Chưa có dữ liệu</td></tr>';$('messagesTable').innerHTML=state.messages.map(m=>`<tr><td>${esc(m.time)}</td><td>${esc(m.sender)}</td><td>${esc(m.group)}</td><td>${esc(m.text)}</td></tr>`).join('')||'<tr><td colspan="4">Chưa có dữ liệu</td></tr>'}document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.view').forEach(x=>x.classList.remove('active'));const id=b.dataset.view;if(id!=='dashboard')$(id).classList.add('active');else $('dashboard').classList.add('active')});$('demo').onclick=demo;$('addGroupBtn').onclick=addGroup;$('save').onclick=()=>{$('msg').textContent='Đã lưu cấu hình trên thiết bị. Access Token không được lưu.'};$('export').onclick=()=>{const rows=[['type','name','id','phone','group','status','time','text'],...state.members.map(m=>['member',m.name,m.uid,m.phone,m.group,'','','']),...state.messages.map(m=>['message',m.sender,'','',m.group,'',m.time,m.text])];const csv=rows.map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(',')).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='lien-hoa-zalo-365.csv';a.click()};document.querySelectorAll('.search').forEach(i=>i.oninput=e=>{const q=e.target.value.toLowerCase();document.querySelectorAll('#'+i.dataset.target+' tr').forEach(r=>r.style.display=r.textContent.toLowerCase().includes(q)?'':'none')});$('dashboard').classList.add('active');render();